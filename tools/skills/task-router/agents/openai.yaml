interface:
  display_name: "任务路由"
  short_description: "先把任务分到正确路径，再决定后面该看什么、做什么。"
  default_prompt: "Use $task-router first. Do not start implementing yet. Infer scope, constraints, and related docs from the task goal and requirements, then classify the request, confirm valid docs, identify reuse paths, and choose the next workflow."
